# Award Audit 项目开发约定

## 真实外部能力调用授权

- 本项目后续开发、调试和验收期间，持续授权调用真实模型 API、真实搜索 API（包括
  AnySearch）以及真实公开网站，用于代码验证、故障定位和基于反馈的迭代修改；无需为每一
  次同类调用重复等待用户确认。
- 调用前应在工作记录中说明案例、调用范围和预期输出；调用后立即检查结果，并以真实反馈
  驱动下一次小范围修改和回归，不能只依赖 mock 或离线测试判断业务正确性。
- 优先按单案例或小批次逐步验证，设置合理的步骤、工具、搜索、候选 URL、token、超时和
  重试预算，避免无界调用、重复费用或对外部站点造成不必要压力。
- 真实调用结果必须如实区分：代码路径验证、外部连通性验证、单案例验证和完整业务验收。
  未完成规定案例的重新验收时，不得将 M5 业务自动审核能力标记为 Go。

## 安全和数据边界

- 上述持续授权不包含破坏性外部操作、向第三方写入业务数据、绕过人工审核、降低
  fail-closed 策略或开始 M6；这些行为仍需单独明确授权。
- 开发代理和诊断命令不得查看、搜索、打印、复制或提交 `.env` 内容。允许项目既有配置
  加载器仅在真实 API 运行时读取 `.env` 并把凭据交给客户端；不得把凭据值返回给代理或
  写入日志、测试产物、异常和文档，所有敏感字段必须脱敏。
- 真实验证使用独立测试目录和数据库，不得修改 `award_audit.db`。真实材料、数据库、响应
  原文和探针产物只放在被 Git 忽略的 `tmp/` 或 `out/`，不得提交。
- 保持域名白名单、内容类型识别、预算、证据状态机、Verifier 和人工复核边界；不得为了
  提高通过率绕过安全限制。

## 验证节奏

- 先为已知问题补充可重复的离线测试，再做最小代码修改，随后尽快运行相关离线测试和对应
  真实案例；根据真实反馈继续修改，直到该案例达到预期或留下可复现的阻塞证据。
- 每轮记录实际调用范围、关键结果、失败阶段和下一步修改，不把离线 pytest、前端构建或
  Playwright 结果描述为真实业务验收。
- 当前 M5 状态固定为：安全链路和可观测性 Go；业务自动审核能力 No-Go。
  2026-08-01 的 v2.2 统一入口已实际重跑 S01/S02/S03/S06、六案 `m5_regression`
  和一个开发期未见 U01；所有分流案件均保持人工结论，S03/S04 来源权威性、
  S05/S06 完整名单仍是阻塞项。不得开始 M6。
